# Core LLM & Orchestration
langchain>=0.3.0
langchain-groq>=0.2.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.0

# Web Scraping — curl_cffi for Chrome TLS impersonation (bypasses bot detection)
beautifulsoup4>=4.12.0
lxml>=5.2.0

# External Research — pin to avoid breaking API changes
duckduckgo-search>=6.3.0

# Financial Data
# curl_cffi >= 0.7.3 supports chrome131 impersonation (required for Yahoo Finance)
# pandas_datareader: Stooq fallback for stock price history
curl-cffi>=0.7.3
pandas>=2.0.0
pandas-datareader>=0.10.0

# UI & Charts
gradio>=5.0.0
matplotlib>=3.8.0
huggingface_hub>=0.25.2

# Utilities
python-dotenv>=1.0.0
tldextract>=5.1.0
